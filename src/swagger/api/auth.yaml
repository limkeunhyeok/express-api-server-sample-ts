tags:
- name: auth
  description: auth API

paths:
  /api/auth/signUp:
    post:
      tags:
      - auth
      summary: user sign up
      responses:
        200:
          description: 'OK'
        500:
          description: 'Server Error'
  /api/auth/signIn:
    post:
      tags:
      - auth
      summary: user sign in
      responses:
        200:
          description: 'OK'
        500:
          description: 'Server Error'